# Validation matrix

Status values are exactly as defined in the specification (§30):
`NOT_STARTED` · `IN_PROGRESS` · `PASS` · `FAIL` · `LIMITED`.

**`LIMITED` is never reported as `PASS`.** A row is `PASS` only when it has been checked
against an independent reference, not merely when the code runs.

Last updated: 2026-09-21 · after gate G4 PASS: drag surface rebuilt as `cfd_surface_v2`, evaluator
switched to Fidelity 1, DOE and M4 re-run (rows G5, M3, M4). Previously: 2026-09-20 (TPI study,
M8 support package, sourcing-report closeout of G1A′, G1B and G2).

| Gate | Subsystem | Verification method | Validation source | Tolerance | Status | Evidence |
|---|---|---|---|---|---|---|
| G1A | Atmosphere, 0–86 km | Exact integration of the defined USSA-76 layer profile; ideal-gas closure; monotonic density | Published USSA-76 layer-base T and p values | 0.01% on T, 0.1% on p | **PASS** | `tests/test_atmosphere.py::test_layer_boundaries_against_published_table` |
| G1A′ | Atmosphere, 86–150 km | Log-interpolation of a transcribed table; runtime `extrapolated` flag; refusal above 150 km; row-by-row diff of the transcribed table against a primary copy | **Primary** *U.S. Standard Atmosphere, 1976* (NOAA-S/T 76-1562 / NASA-TM-X-74335), Table I "Geometric Altitude, Metric Units", pp. 68–69, read off 300 dpi page renders — at **90, 100, 110, 120 and 150 km only** | Half of the primary's own printed resolution: 0.005 K on T, 2.5×10⁻⁴ rel. on ρ. **Measured: exact agreement to every printed digit**, T and ρ, at all five altitudes. No table value was changed | **PASS** (transcription, at the five primary-checked altitudes) / **LIMITED** (everything else) | `tests/test_atmosphere.py::test_upper_table_against_primary_ussa76`; see ASSUMPTIONS A-ATM-2. **Still LIMITED for:** the 86, 95 and 130 km rows (no primary value obtained); the pressure column at every altitude; and the log-interpolation *between* rows, which is not the species-diffusion model USSA-76 actually defines above 86 km. NR-14 makes this material — 5–20% of a feasible design's integrated heat load accrues here |
| G1B | Trajectory | Integrator tolerance convergence; physical-ordering tests (steeper ⇒ shorter and higher g); ballistic-coefficient ordering; no nonphysical states. **Added 2026-09-20:** comparison against an independently-implemented Allen–Eggers closed form under matched assumptions, on three published entry cases | Allen & Eggers, **NACA Report 1381** (1958), NTRS 19930091020 — closed form. Benchmark: **Putnam & Braun, JGCD 38(3) 2015, Table 2 p. 419**, which runs the same three cases through both the closed form and a full numerical integration and publishes the disagreement | **The published disagreement is the expected result**, not zero — Allen–Eggers neglects gravity and holds γ constant, so a correct integrator must disagree, increasingly as entry flattens. Allowance on reproducing each published figure: **1.0 percentage point**, which is the largest residual measured once every published assumption is matched, not a physics tolerance | **PASS** | `tests/test_trajectory_allen_eggers.py` (10 tests) + `tests/test_trajectory.py` (7). **Measured, (AE − numerical)/numerical, ours vs. published:** peak deceleration −5.89% vs −5.1% (γ₀ = −30°), +35.82% vs +34.9% (−8.2°), −58.18% vs −57.3% (−1.35°); velocity at peak −1.78/−2.29/+35.25% vs −1.8/−2.1/+34.9%; altitude at peak +3.56/−4.73/+27.30% vs +3.6/−4.6/+27.0%. Max residual 0.92 points. Matched assumptions: ρ = 1.215 e^(−h/8500), g = 9.81 const., R = 6378 km, L/D = 0, non-rotating (A-ATM-5, A-TRAJ-5) |
| G2 | Aeroheating — the constant | Exact V³, √ρ and 1/√R_n scaling; direct reference evaluation; input-domain rejection. **Added 2026-09-20:** the constant re-derived from the primary, and TR R-376 eq. (33) evaluated end-to-end in the report's own units (MW/m², atm, m, MJ/kg) | **Primary** NASA TR R-376 (Sutton & Graves, 1971), NTRS 19720003329: eq. (33) p. 13, SYMBOLS units pp. 2–3, K(air) = 0.1113 from Table II p. 39, all read off 300 dpi renders. **The report contains neither the V³ form nor 1.7415×10⁻⁴** | 1e-12 on scaling ratios. On the constant: **the primary's own average correlation error for air, 3.3%** (its Table II) — the tolerance is the source's, not this project's. **Measured: 0.39%** | **PASS** | `tests/test_heating.py` (10 tests); derivation `docs/theory/sutton_graves_constant.md`; decision not to change the value `docs/negative_results.md` NR-12; see ASSUMPTIONS A-HEAT-1 |
| G2′ | Aeroheating — model form | *Not attempted.* | — | — | **LIMITED** | Catalycity is the largest unquantified term (equilibrium-boundary-layer basis ≈ fully catalytic; catalytic flux reported ~2× non-catalytic, 🟡 T2 ONERA/CNES 2019), the cold-wall assumption is applied to a ~2400 K surface, radiative heating is neglected, and the code does not reject inputs outside the primary's fitted domain (2.3–116.2 MJ/kg, 0.001–100 atm). Tauber's review papers (NASA TP-2914 1989; Tauber & Sutton 1991) remain unread (❌ T3). These are carried as assumptions A-HEAT-2..4, and **G2 being PASS says nothing about them** |
| G3 | TPS conduction | Analytical benchmark at four depths and at the surface; grid-refinement convergence; timestep-refinement convergence; energy-balance closure; null test; physical-ordering tests | Carslaw & Jaeger, *Conduction of Heat in Solids* 2nd ed. §2.9, semi-infinite solid under constant surface flux | 0.2% against analytical; energy residual < 1e-6 | **PASS** | `tests/test_tps.py` (11 tests). Measured: 0.002% surface error, energy residual ~1e-14. |
| G3b | TPS conduction — bench boundary options | Closed-form steady state with a convective back face and a contact resistance; lumped Newton-cooling decay for the front-face loss; energy closure with all four sinks active; regression test that the defaults leave the entry model bit-identical | Closed-form solutions only. Added 2026-09-20 for the M8 coupon; these terms are **off by default** and no M1/M1b result uses them | 1e-4 rel. on the steady state; 2e-3 rel. on the lumped decay; energy residual < 1e-4 | **PASS** (internal) | `tests/test_tps_boundaries.py` (12 tests); see ASSUMPTIONS A-TPS-8 |
| G-GEO | Parametric capsule geometry (Phase F) | Closed-form spherical-cap/cone-frustum checks (hemisphere, full-sphere, cone, cylinder limits); independent numerical washer integration of `.profile()` cross-checked against the closed-form + quadrature volume; tangent-continuity at all 3 internal joins by finite difference; STL watertightness (edge-count) and mesh volume (divergence theorem) both computed by reading the exported file back, not assumed from construction; byte-identical determinism | Internal cross-checks only (textbook closed forms + an independent numerical method each time) - no external/published capsule geometry has been reproduced | 1e-12 on closed-form building blocks; 1e-5 rel. on profile-integrated vs. closed-form+quadrature volume; 5e-3 rel. on STL mesh volume vs. closed-form volume; 0.05 deg on join tangent angles | **PASS** (internal) / **LIMITED** (no external geometry reference) | `tests/test_geometry.py` (42 tests); see ASSUMPTIONS A-GEO-1..9. Every check here is against another computation this project derived, not an outside reference - genuinely `PASS` for internal consistency, `LIMITED` for the row as a whole until a published capsule's moldline is reproduced. |
| G-GEO2 | Effective nose radius (stagnation velocity gradient) | Exactness at all nine tabulated points; the two analytic limits (hemisphere R_eff -> R_n to 1e-12, flat face saturating at 3.155 R_b); monotonicity in both R_n and R_c; continuity in K including across the K = 1 seam where the model hands back to the cap radius; module table pinned against its provenance file; persisted M1 results reproduced bit-for-bit under the legacy default | **Primary** NASA TN D-5121 (Ellison, 1969), NTRS 19690013192, **Table I** alpha = 0 rows, transcribed from a 400 dpi page render - nine measured values of R_B/R_eff at M = 8, Re_D = 1.37e6. Cross-checked against **primary** NASA TM X-1067 (Zoby & Sullivan, 1965), NTRS 19660017753, whose eqs. (3)-(5) define R_eff and whose figure 4 was digitised (+-0.010, T2, cross-check only) | **The tolerance is the sources' own disagreement**, not this project's: Ellison p. 5 states agreement with Zoby & Sullivan within **10%** at K = 0 and K = 0.707 and about **20%** at K = 0.417. That is ~+-10% on heat flux. Exact (1e-12) at the tabulated points and at the hemisphere limit | **PASS** (implementation and limits) / **LIMITED** (the physics) | `tests/test_stagnation_gradient.py` (49 tests); derivation `docs/theory/effective_nose_radius.md`; provenance `data/reference/stagnation_velocity_gradient.yaml`; NR-15's 2026-09-20 follow-up; ASSUMPTIONS A-GEO-3, A-GEO-3a, A-OPT-5. **LIMITED because:** every number is alpha = 0 and M <= 8 cold-wall perfect-gas, transferred to a 7.4 km/s real-air flight condition purely on Zoby & Sullivan's pressure-distribution-invariance argument; the interpolation between the nine points is **this project's construction**, not a published fit; K > 1 (sphere-cones) is outside both sources and falls back to R_eff = R_n, flagged; and no AETHER geometry has been compared against a measured velocity gradient of its own |
| M1 | Burn-vs-bake result | Full-domain sweep; automated signature-pair search with pre-declared thresholds; rank correlation | Self-consistent within the model. No external validation exists for this claim. | — | **IN_PROGRESS** | `reports/milestones/M1_burn_vs_bake.md`. The *mechanism* is verified; the *magnitude* is model-dependent and awaits Fidelity 1 and the physical coupon experiment. |
| G4 | OpenFOAM CFD | Mesh independence (three meshes, r = 2, GCI per the three-grid procedure), force convergence (declared window criterion on forebody C_D, every benchmark case), published blunt-body benchmark, model-form limitations; a full-body negative case that must FAIL the criterion, and does; gate bookkeeping unit-tested without OpenFOAM | Sphere, Mach 3 and 6, inviscid perfect gas: exact Rayleigh-pitot p0; Van Dyke & Gordon NASA TR R-1 stand-off; Clark's forebody pressure-drag expression (AEDC-TR-70-291). **Total sphere drag is NOT compared like-for-like** (no base pressure is computed) | p0 ±1%, stand-off ±5%, forebody C_D ±3%; GCI_fine ≤ 2%, fine–medium ≤ 1%, monotone; force peak-to-peak ≤ 0.1% and drift ≤ 0.02% over 2000 iterations | **PASS** | Status copied from `results/M2/M2-20260920T123901Z/gate_assessment.json`, never set by hand. **Read with its history:** on the original runs the file said LIMITED, because both fine cases ended in a bounded C_D limit cycle outside the peak-to-peak limit (`gate_assessment_20260921_0057_before_restarts.json`). The criterion was not changed; the fine cases were continued at a lower Courant limit (spec §36) and met it in two consecutive windows. That restart rule and the solution-of-record rule were written AFTER the original results were seen (NR-25) - whether that history permits downstream use is the project owner's call, not this row's. Also: Mach 6 margin is thin (0.077% of 0.1%); observed order of C_D is about 1 and weakly determined; p0 and stand-off are single-snapshot values; Billig exponent 3.24 vs 3.2 unresolved; GCI procedure's primary source not opened (sourcing report item 9); OpenFOAM v2512, not v2606. `reports/milestones/M2_cfd_validation.md`, `tests/test_cfd_*.py`. NR-05…NR-09, NR-25, NR-26. |
| G5 | Coupled model | End-to-end evaluator determinism and provenance with the CFD drag surface ON: same config → identical result fingerprint (every metric, margin, diagnostic, provenance block and the velocity history, bit for bit); result records surface training hash, source run, mesh level, G4 status at build AND at evaluation, shape-extrapolation flag, share of heat load / flight time outside the CFD Mach range; a shape outside the CFD hull is a rejected candidate, not an extrapolated number; a provisional surface is refused without `allow_provisional`; the constant-C_D path is bit-for-bit untouched | None external. Internal comparison only: constant C_D vs surface on the baseline capsule and M4-front designs | exact equality of fingerprints | **PASS** (software gate only) | All six determinism/provenance checks met on `cfd_surface_v2`, which was built AND evaluated with G4 = PASS, both READ from files (`results/M3/M3-DP-20260920T1610Z/coupled/gate_G5.json`; `tests/test_aero_surface.py`, including: a PASS surface is served without opt-in and refused again the moment the gate file stops saying PASS; no gate status is typed in source). **Scope:** G5's reference is exact self-equality - it says the coupled evaluator is deterministic and labels its aerodynamics. It says NOTHING about whether the drag is right; that is row M3, which stays LIMITED, and G5 inherits NR-25's caveat about how G4 became PASS. `reports/milestones/M3_coupled_model.md` §10; v1 report archived as `M3_coupled_model_surface_v1.md`. |
| M3 | CFD-derived drag response surface `cfd_surface_v1` (GP of C_D,fore over log Mach, R_n/D, cone half-angle, R_c/D; + assumed base-drag band) | Seeded design, deterministic; every design point a valid capsule; invalid draws logged; acceptance rules (force criterion unchanged from M2, supersonic outflow, automatic bow-shock clearance on and off axis) unit-tested; scale invariance measured on a two-diameter pair; stagnation pressure vs exact Rayleigh-pitot on every case; held-out test set drawn before any case ran; k-fold CV with RMSE/MAE/R²/interval coverage, inside- and outside-hull reported separately; persisted surface reloads bit-for-bit and refuses a hand-edited table; GCI hook reads `results/M2/<run>/gci.csv`, never a typed number | **None for capsules.** The solver's only external validation is M2's sphere (row G4), at Mach 3 and 6; between Mach 6 and 20 the only per-case check is the exact stagnation pressure. Base drag: band from the exact vacuum limit and NASA TN D-4800 fig. 11(b) (opened; read off a graph by eye) — an assumption, not a validation | none declared for accuracy; the report prints what was measured | **LIMITED** | `cfd_surface_v2`: no longer provisional (G4 PASS at build and now, read), 69 usable points, Mach 3–27. Measured: k-fold RMSE 0.0130 in C_D (1.1% of mean; inside-fold-hull 0.0151), held-out 8/8 usable RMSE 0.0234, R² 0.996, held-out z-spread 1.70 vs sigma inflation 1.21 applied - still somewhat overconfident on n = 8; coarse→medium change ≤ 0.41% on 6 capsule pairs; discretisation band 0.63% from a SPHERE's GCI. NR-21's six cases retried with M2's Courant lever: 4 recovered, 2 not (NR-27, incl. a one-window pass caught and refused); Mach 27 node: 9 of 14 anchors, slender cones fail at the limiter hand-over (NR-28); heat load flown above the top node 53.7–66.5% → 0.0–2.9%. **Why still LIMITED:** no capsule validation data at all; coarse mesh (A-CFD-10); perfect gas is the wrong gas at these speeds - running it to Mach 27 closes an extrapolation in Mach, NOT the model-form error, which stays a declared, unvalidated ±5%; base drag assumed; per-case iterative scatter up to ~0.2% (NR-27). `reports/milestones/M3_coupled_model.md`, `results/M3/M3-DP-20260920T1610Z/` (`v1_tables/` = v1), `tests/test_cfd_design_points.py`, `tests/test_aero_surface.py`. NR-19…NR-22, NR-27, NR-28. |
| M8 | Physical thermal coupon | Blind prediction, frozen before measurement (spec §47) | **No measurement exists.** Synthetic twin only: data generated by this project's own solver | Tolerance to be declared before the runs, per `protocol.md` §9 | **IN_PROGRESS** | Support package and synthetic chain verified; physical experiment not yet run. `experiments/thermal_coupon/`, `tests/test_thermal_coupon.py` |
| §44 | Thermal Penetration Index | Rank-redundancy test against peak bondline T and integrated heat, thresholds pre-declared in `configs/tpi_study.yaml` | Self-consistent within the model; prior art reviewed in `docs/theory/tpi.md` | rank R² ≥ 0.98 ⇒ discard | **LIMITED** | `reports/milestones/TPI_study.md`. Verdict **DISCARD** on this design space; see `docs/negative_results.md` NR-05. |
| M4 | DOE, sensitivity, Pareto optimisation infrastructure | Dominance, 2-D hypervolume, spacing and knee against hand-worked cases; Sobol' estimator against the Ishigami function's analytical indices; LHS one-point-per-stratum property; candidate-log CSV/Parquet round trip; budget wrapper spends exactly its budget for every method (duplicates free, invalid geometry charged); identical candidate log under a fixed seed; full-geometry evaluator path reproduces the legacy two-parameter result bit-for-bit; per-design artefact audit (bounds, active constraints, >86 km heat-load share, soak-out truncation) | Algorithms: analytical test cases only. **Results: none — Fidelity 0 (constant C_D), no CFD-derived input, no external optimisation benchmark reproduced** | 1e-12 on hand-worked Pareto cases; 0.03 abs. on Ishigami indices at n_base = 4096 | **LIMITED** | `tests/test_pareto.py`, `tests/test_optimization.py`. **Re-run at Fidelity 1** (`cfd_surface_v2` + velocity-gradient nose model): DOE `M4-DOE-20260920T204844Z`, optimisation `M4-OPT-20260920T205252Z`, `reports/milestones/M4_pareto_optimisation.md` (Fidelity-0 report archived as `…_fidelity0.md`). NSGA-II HV 0.347 ± 0.006, scalarised DE 0.300 ± 0.015, LHS floor 0.287 ± 0.021 (7 seeds × 1000). **Why LIMITED, not PASS:** the machinery is verified against analytical cases, but the RESULT has no external reference, and the audit (NR-29, NR-30) shows the front is a flight-path-angle trade-off at a geometry set by fences - mass closure at ~0.99 (unsourced budget), the CFD hull edge at R_n/D 1.2, a frozen shoulder whose sharp-corner lever (≈7% of peak flux) a stagnation-point-only heating model would reward for the wrong reason, and 15–21% of heat load in the flagged >86 km atmosphere. H1 supported within the model in absolute terms (knee −14.9 K, +21.5 kW/m² vs the peak-flux-only optimum). Open: NR-14, NR-29, NR-30. |
| M5 | AI vs conventional optimiser ablation (harness + study) | Strict §23 schema: every rejection reason pinned, out-of-bounds never clipped; hard per-seed and study-wide LLM call caps; hung/missing CLI is a failed call, not a crash; prompts and raw responses persisted and a strict replay reproduces the candidate log exactly (fake CLI in tests — no test makes a live call); exact permutation and signed-rank tests against their known extreme values, Holm against a hand-worked case; GP accuracy and interval coverage on a synthetic function; extrapolation outside the training hull raises; promotion policy never grants a fidelity that does not exist; source-tree hash guard and stale-screening guard; report generated end-to-end offline with a non-M4 active-variable list | **No independent reference exists for an optimiser comparison.** Study run `M5-ABL-20260920T211134Z` (Fidelity 1, `cfd_surface_v2`, 200 evaluations x 5 seeds, 67 live LLM calls of a cap of 80, 0 failed or malformed). Internal checks only: strict replay `M5-REPLAY-20260920T224633Z` with no LLM access reproduced every method's per-seed hypervolume exactly (0 prompt mismatches, same source hash); M5's `nsga2` reproduces the first 200 evaluations of M4's runs on the shared seeds to 0.0 in hypervolume; the agent's round-1 geometry rule checked against all 5400 logged designs | exact equality of replayed candidate logs; 1e-12 on statistics | **LIMITED** | Study run once on the final physics, replay-verified, qualitative audit written. Pre-declared rule (A-AI-7) applied as written: **AI helped at 50 and 100 evaluations, no measured difference at 200** (agent ahead of `bo_parego` in all 5 seeds at 200, Holm p 0.0119, but mean gain +0.0036 is under the declared 0.005). LIMITED because n = 5, one LLM, one easy landscape, priors not separable from reasoning, and about half the final lead is precision on placeholder limits (NR-32). Three generator sentences need a caveat at Fidelity 1 (NR-31). `reports/milestones/M5_ai_ablation.md`, `M5_qualitative_audit.md`; `results/M5/M5-ABL-20260920T211134Z/` (+ `audit/`). `tests/test_ai_agent.py`, `tests/test_surrogate.py`; `configs/ai_ablation.yaml`; `make ablation`. The earlier aborted run (NR-18) must not be analysed. |
| M6 | Adaptive-fidelity optimisation (harness) | With a FAKE F1 of known analytic truth (no OpenFOAM, no LLM in any test): F1 refuses to run while G4 is not PASS unless `allow_provisional`; evaluations and CFD calls counted separately and exactly (a design is charged once per surface version, cache hits free, re-evaluations charged, probes counted and never logged); a failed CFD case costs a call, is kept and yields no training point; a repeat within an arm is free, a case two arms request runs once, concurrency cap observed; promoting an out-of-hull design at both Mach ends extends the hull and makes it AND its neighbours evaluable; the policy keeps clearly-poor and already-trusted candidates at F0; the LLM agent's `requested_fidelity` opens the 'matters' gate but not the budget, and the `ai_adaptive` arm replays strictly from recorded responses; scheduled comparators spend their budget, random is seeded, up-front spends before the first evaluation; pooled truth pools every arm and excludes hold-out cases; a deliberately flattering starting surface yields a positive optimism gap and no score; the curve scores only the recommended set and drops false claims (hand-computed hypervolume); the H2 rule returns SUPPORTED / CONTRADICTED / NO_MEASURED_DIFFERENCE / NOT_SUPPORTED / NOT_TESTABLE on constructed cases; hull membership of a boundary design is independent of the triangulation (NR-23); M5 and M6 runners share one guard implementation, which aborts AFTER logging what was paid for; the real `evaluate_design` sees a refit | **None. The study has not been run.** One real end-to-end smoke test on the development seed with `allow_provisional: true` (`results/M6/M6-SMOKE-20260920T180010Z`): 2 OpenFOAM promotions through M3's design-point runner, run one at a time, both USABLE; surface refitted 56 → 57 → 58 points; both promoted candidates re-evaluated under the new surface; 38 search + 2 re-evaluations = 40 evaluations, 2/2 CFD calls. It is a plumbing check, not a result | exact equality of budgets, ledgers and replayed logs; hand-computed hypervolume to 1e-12 | **IN_PROGRESS** | Harness built; study not run. `tests/test_adaptive_fidelity.py` (24 tests); `configs/adaptive_fidelity.yaml` (policy, comparators, study size and the H2 criterion, all declared before any run); `make adaptive`. Blocked on gate G4 = PASS and a re-run of DOE/M4 on `cfd_surface_v1`; the runner refuses otherwise. No M6 report or figure exists under `reports/` by design. NR-23, NR-24. |
| M7 | Uncertainty propagation and robust design (harness) | Distribution sampling reproducible from the seed alone and inverse-CDF correct against scipy; a distribution family REFUSES parameters it does not take (`{family: normal, low: 0, high: 1}` would otherwise silently sample N(0,1)); Wilson interval against its own algebra and Clopper-Pearson at k = 0 against the closed form 1 - alpha^(1/n) (the rule of three); percentile against the order statistic that defines it; the whole propagation chain against a TOY LINEAR MODEL whose mean, variance and percentiles are analytic, and whose exceedance probability is recovered inside its own confidence interval; nested draws proven to share common random numbers across epistemic branches and to hold the epistemic block fixed within one; every physics hook (density profile, Sutton-Graves scale, >86 km flux multiplier) proven bit-identical to the pre-M7 evaluator at its default; the density perturbation proven to preserve p = rhoRT and the speed of sound exactly; the run REFUSES when the C_D discretisation term's GCI is unavailable, naming the input and the requirement; a T3 input is refused unless its source line says 'judgment'; chance-constraint arithmetic, not-evaluable draws counting as violations, and the inner sample's probability RESOLUTION; robust NSGA-II spends exactly its budget through the same `BudgetedEvaluator` and never optimises the draw coordinate; `verify_shortcut` proven to FAIL on an injected bias, a reversed ranking and a wrong violation probability; the report generated end to end offline from a summary alone; a missing source becomes NOT AVAILABLE and never a substituted design | **None. The study has not been run.** Two end-to-end smoke runs at tiny sample sizes prove the chain executes: `results/M7/M7-SMOKE-ROBUST-*` (robust NSGA-II, 15 generations, 960 inner evaluations, 4 chance-feasible front designs, shortcut verification PASSED on 3 designs) and `results/M7/M7-SMOKE-*` (4 designs propagated, Sobol' attribution, all 7 figures, the §39 table). Both are plumbing checks, not results, are labelled `SMOKE.md` in their own directories, and publish nothing to `reports/` | exact equality of the identity-default evaluator; 1e-12 on the interval algebra; the analytic linear model to 2e-3 on the mean and 0.05 sigma on each percentile | **IN_PROGRESS** | harness built; study not run. `tests/test_uncertainty.py` (45 tests) + `tests/test_robust.py` (16); `configs/uncertainty.yaml` (every input with its source line and tier, the robust problem, the chance-constraint levels, the shortcut tolerances and the study size, all declared before any run); `docs/theory/uncertainty.md`; `make uncertainty` / `make robust`. **Blocked on a re-run of `make doe && make optimize`**: the runner refuses because the current DOE screening is void for today's design space, and the M4 front it compares against was computed under the legacy `cap_radius` heating model behind a bluntness cap since removed (A-OPT-5, NR-15). No M7 report or figure exists under `reports/` by design. ASSUMPTIONS A-UQ-*. |
| M7 | Uncertainty propagation and robust design (STUDY, run 2026-09-21) | 4 designs x 3000 nested draws (24 epistemic branches x 125 shared aleatory draws) through `evaluate_design` at Fidelity 1; Saltelli attribution on `baseline` and `joint_knee` (2 x 3,584); robust NSGA-II, 3 seeds x 30,000 inner evaluations; the 32-draw CRN shortcut re-scored on 8 front designs with 1000 fresh draws; paired same-draw differences between designs (`scripts/analyse_m7_paired.py`, reads files only) | Internal only: the shortcut against independent full Monte Carlo. NO external reference for any output distribution; 7 of 12 inputs are T3 engineering judgment; gate G2' has no distribution, so every spread is a lower bound | Pre-declared in `configs/uncertainty.yaml`: shortcut \|bias\| <= 5%, Spearman >= 0.80, \|dP\| <= 0.05; statistics' bootstrap half-width <= 1% | **LIMITED** | Shortcut verification **PASSED as declared** (bias -1.61% / +0.32%, Spearman 1.000 / 1.000, max \|dP\| 2.6%); 1 of 8 checked designs reads P(any) 5.7% > 5% on fresh draws, and all 46 front designs sit at exactly 1/32 on the mass-fraction chance constraint (NR-33). Convergence tolerance met as declared (<= 0.09%); a branch-level bootstrap gives 0.4-1.06% and the baseline mean marginally misses 1% (NR-34). Knee - peak-flux-only bondline: -14.31 K, s.d. 1.45 K, negative in 3000/3000 shared draws. Nominal optima P(any violation) 29.6-46.6%, bondline 0/3000 (< 0.13%). LIMITED because nothing is validated against an independent reference, the attribution figure is mislabelled and the report carries wrong generator sentences (NR-34) that cannot be fixed while M5/M6 hold the source tree. `reports/milestones/M7_uncertainty_robust.md`, `M7_addendum_posthoc.md`; `results/M7/M7-ROBUST-20260920T211216Z/`, `results/M7/M7-UQ-20260920T233048Z/` (+ `paired_difference.json`). |

## What the current evidence does and does not support

**Supported.** Within this model, over the tested domain, peak external heat flux and
peak bondline temperature are perfectly anti-ordered (Spearman ρ = −1.0), and a joint
objective selects a design with materially more bondline margin than a peak-flux-only
objective does. The conduction solver underlying that claim agrees with an analytical
benchmark to 0.002% and closes its energy balance to ~10⁻¹⁴.

**Not supported.** Any statement about a real vehicle, a real TPS material, or a flight
condition. The aerodynamics are a constant coefficient and no CFD or experimental
validation exists yet.

### The three 2026-09-20 upgrades, and what they did *not* buy

G1A′, G1B and G2 moved off `LIMITED` on the strength of the sourcing pass. Each upgrade is
narrower than the row title suggests, and the narrowing is the point:

- **G1A′** validates a *transcription*, at five altitudes, and nothing else. The
  interpolation between those altitudes is still a log-linear stand-in for USSA-76's
  species-diffusion model, and NR-14 shows the bondline objective draws 5–20% of its heat
  load from exactly that region. The row is `PASS` for the five rows checked and `LIMITED`
  everywhere else, deliberately written that way rather than rounded up.
- **G1B** now reproduces a published integrator-vs-closed-form comparison to within 0.92
  percentage points — but under an *exponential* atmosphere, constant gravity and a
  constant C_D, because those are the assumptions under which a closed form exists. It
  validates the integration, not the production configuration, and no flight trajectory
  has been reproduced.
- **G2** validates the *constant*, to 0.39% against a re-derivation from the primary. It
  says nothing about whether Sutton–Graves is the right correlation for this problem;
  that is G2′, which is untouched, and where catalycity alone is worth a factor of about
  two. Treat the correlation as ±4% at best (A-HEAT-1, NR-12).

Two constraint limits were also sourced without being changed: the 450 K bondline
allowable is the Space Shuttle Orbiter's aluminium-structure design limit (A-LIM-1a), and
the 12 g deceleration limit turns out to correspond to **no** documented sustained-g curve
(A-LIM-1b). The second is left as an explicit open student decision with its measured
effect on the feasible region, because adopting the NASA-STD-3001 deconditioned curve
would empty the M1b design space entirely — a result, not a tuning knob.

### M8 and §44, added 2026-09-20

**M8 is `IN_PROGRESS`, and specifically it is not `PASS`.** The support package is
complete — printable holder, dimensioned sensor drawing, data schema, safety and
calibration protocol, calibration and comparison code, and the archived-prediction
machinery §47 requires. The calibrate → freeze → predict → compare chain has been
verified end to end against **synthetic data generated by this project's own solver**,
which is a check on the software and nothing else. No coupon has been printed, no heater
has been switched on, and no measurement exists. The row moves off `IN_PROGRESS` only
when a real blind comparison has been written against a tolerance declared beforehand.

**§44 is `LIMITED` with a verdict of DISCARD.** The Thermal Penetration Index was
implemented, tested against closed-form cases, and evaluated over the M1b design grid
against a redundancy criterion declared in advance. Its ranking of designs is 99.94%
reconstructible from peak bondline temperature and integrated heat load, so it adds no
interpretable information on this design space and is not adopted. That is a result
about *this* stack and *this* two-variable space, which is why the row is `LIMITED` and
not `FAIL`.
